#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;
ll n, xg, yg;

int main() {
	cin >> n >> xg >> yg;
	vector<ll> x(n), y(n);
	for (int i = 0; i < n; i++) {
		cin >> x[i] >> y[i];
		
	}
	return 0;
}
